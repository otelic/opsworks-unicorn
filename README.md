Updated unicorn nginx template
==============================

This is used to serve the static assets with a long Expires header,
and to have nginx serve the pre-gzipped asset if the client supports gzip.
